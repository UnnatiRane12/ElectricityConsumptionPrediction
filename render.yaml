services:
  - type: web
    name: powerpredict-backend
    env: python
    region: oregon
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:oOybjq4kuGbVV5uc@db.rfhapnpecrbpdrudzyek.supabase.co:5432/postgres
      - key: SECRET_KEY
        value: powerpredict_super_secret_jwt_key_2026_energy_ai
      - key: PYTHON_VERSION
        value: 3.11.0
